# CLAUDE.md — KinderDash

Claude should follow the repository workflow documented in `AGENTS.md`.

Do not duplicate project rules here. `AGENTS.md` is the single source of truth for:

- what this fork is and is not (Android only; iOS inherited and out of scope)
- branch and commit strategy
- build and verification policy
- decisions that look wrong without their reasons — read these before "fixing" anything
- widget layout constraints
- security rules (this repository is public)

Before making changes, read `AGENTS.md` from the repository root and follow it.
